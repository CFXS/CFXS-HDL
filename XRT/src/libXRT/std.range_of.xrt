namespace std {

    // Get bit range required to represent value of x
    template<typename T>
    verify<std::is_integral<T>(), "T is not an integral type">
    extern std::range length_of(T x);

}
